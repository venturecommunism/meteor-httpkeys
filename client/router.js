/*
RemoteRegisterController = RouteController.extend({
  run: function () {
    var callback = this.params.callback;
    console.log('callback: ' + this.params.callback);
  }
});



Router.map(function () {
    this.route('remoteboard', {
        path: '/register',
        action: 'someAction',
        controller: 'RemoteRegisterController'
    }, function () {
       console.log('the form is: ' + this.params.form)
       console.log('the public key is: ' + this.params.publickey)
       console.log('the nonce is: ' + this.params.responsenonce)
       console.log('the callback is: ' + this.params.registrationcallback)
    });

});


*/
